#include "pch.h"
#include <tinyxml2/tinyxml2.h>

bool daeconv_perform(const char* inFileName, const char* outFileName, ConversionOptions options)
{
	print("ERROR: DAE to LMF conversion not supported at current moment!" NL);
	return (false);
}